# Binary Persistency Tests

This folder contains Python tests strictly dedicated to verifying binary persistency in the MPS CLI ('*.mpb' files).

## Scope
- Tests here are limited to binary format validation.
- These are kept separate to ensure specialized validation of the mps-cli-py binary support.