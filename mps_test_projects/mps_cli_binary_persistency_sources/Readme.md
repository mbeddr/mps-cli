This project contains the solutions used to create and package the library example in binary persistency format.

- `mps_cli_lanuse_binary_persistency.build` - build solution which when ran generated in the 'build' directory the models in binary persistency
