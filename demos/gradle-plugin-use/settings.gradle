pluginManagement {
    repositories {
        maven {
            name = "GitHubPackages"
            url = uri("https://maven.pkg.github.com/mbeddr/mps-cli")
            credentials {
                username = github_username
                password = github_token
            }
        }
    }
}